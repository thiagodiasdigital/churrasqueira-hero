import { activeClient } from "@/content/clients";

export const empresa = activeClient.empresa;
export const homeContent = activeClient.homeContent;
export const produtos = activeClient.produtos;
export const regioes = activeClient.regioes;
export const depoimentos = activeClient.depoimentos;
export const theme = activeClient.theme;
